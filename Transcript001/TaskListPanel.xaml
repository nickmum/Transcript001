<UserControl x:Class="Transcript001.TaskListPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Transcript001"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Tasks" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>

        <!-- Add task row -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="NewTaskTextBox" Grid.Column="0" VerticalContentAlignment="Center"
                     Margin="0,0,5,0" KeyDown="NewTaskTextBox_KeyDown"/>
            <ComboBox x:Name="CategoryComboBox" Grid.Column="1" Width="85" Margin="0,0,5,0"
                      VerticalContentAlignment="Center">
                <ComboBoxItem Content="Quick" IsSelected="True"/>
                <ComboBoxItem Content="Medium"/>
                <ComboBoxItem Content="Long"/>
            </ComboBox>
            <Button Grid.Column="2" Content="Add" Padding="10,3" Click="AddTaskButton_Click"/>
        </Grid>

        <!-- Task list -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="TasksItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:TaskItem}">
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="0,0,5,0"
                                      IsChecked="{Binding IsCompleted, Mode=TwoWay}"/>

                            <TextBlock Grid.Column="1" Text="{Binding Text}" TextWrapping="Wrap"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                <Setter Property="Foreground" Value="Gray"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- Category chip with duration range -->
                            <Border Grid.Column="2" CornerRadius="8" Padding="6,1" Margin="5,0"
                                    VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Category}" Value="Quick">
                                                <Setter Property="Background" Value="#FF5BA044"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Category}" Value="Medium">
                                                <Setter Property="Background" Value="#FFE08F1B"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Category}" Value="Long">
                                                <Setter Property="Background" Value="#FFEF2525"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Foreground="White" FontSize="11">
                                    <Run Text="{Binding Category, Mode=OneWay}"/><Run Text=" · "/><Run Text="{Binding DurationDisplay, Mode=OneWay}"/>
                                </TextBlock>
                            </Border>

                            <Button Grid.Column="3" Content="✕" Padding="5,0" VerticalAlignment="Center"
                                    Click="RemoveTaskButton_Click"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
