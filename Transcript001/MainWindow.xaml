<Window x:Class="Transcript001.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Video Transcript Processor" Height="800" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="UrlTextBox" Grid.Row="0" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
        <ComboBox x:Name="FormatComboBox" Grid.Row="1" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Left" Width="200">
            <ComboBoxItem Content="Plain Text" IsSelected="True"/>
            <ComboBoxItem Content="Time-stamped Text"/>
            <ComboBoxItem Content="YouTube Original"/>
        </ComboBox>
        <Button Content="Process Video" Click="ProcessVideo_Click" Grid.Row="2" Margin="10" Padding="10,5"/>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <wv2:WebView2 x:Name="VideoPlayer" Grid.Column="0"/>
            <ListView x:Name="TranscriptListView" Grid.Column="1" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Text}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                            <Setter Property="Background" Value="Yellow"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <TextBox x:Name="LogTextBox" Grid.Row="4" Margin="10" IsReadOnly="True" 
                 VerticalScrollBarVisibility="Visible" 
                 HorizontalScrollBarVisibility="Auto"
                 Height="100"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"/>
        <ProgressBar x:Name="ProgressBar" Grid.Row="5" Height="20" Margin="10"/>
        <TextBlock x:Name="StatusText" Grid.Row="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>